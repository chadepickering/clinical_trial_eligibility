"""
Response generation via Mistral-7B served through Ollama.

Constructs a prompt from retrieved trial context and patient profile,
queries the local Ollama endpoint, and returns a structured response.
"""


def generate_response(context: str, patient_profile: dict) -> str:
    pass
